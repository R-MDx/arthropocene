<div class="login">
  <div class="heading">
    <h2>Change your password</h2>
    <%= simple_form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :put }) do |f| %>
    <%= f.error_notification %>

  <%= f.input :reset_password_token, as: :hidden %>
  <%= f.full_error :reset_password_token %>

  <div class="form-inputs">
    <%= f.input :password, label: "New password", required: true, autofocus: true, hint: ("#{@minimum_password_length} characters minimum" if @minimum_password_length) %>
    <%= f.input :password_confirmation, label: "Confirm your new password", required: true %>
  </div>

  <button type="submit" class="float" name="commit" value="Change my password">Change my password</button>
  </div>
  <% end %>

  <div class="login-links text-center">
      <%= render "devise/shared/links" %>
</div>

===========================================
    <div class="input-group input-group-lg">
      <span class="input-group-addon"><i class="fa fa-user"></i></span>
      <%= f.input :email, required: true, label: false, autofocus: true, placeholder:"Username or email", class: "password optional form-control" %>
    </div>

<!--   <div class="input-group input-group-lg">
    <span class="input-group-addon"><i class="fa fa-lock"></i></span>
  </div> -->


