<h1>Posts</h1>

<ul>
  <% @posts.each do |post| %>
    <li id="post-<%= post.id %>">
      <%= post.title %>
      <%= link_to "Delete", post, method: :delete, remote: true %>
    </li>
  <% end %>
</ul>
